# T2.8 — Local harness, mock LLM, and Gate G2 script
- Agent/session: agent-t2.8   Started: 2026-09-06T22:30:00Z   Finished: 2026-09-06T22:39:00Z   Size est/actual: M / 45m

## Plan
- Implement `runner/main.ts` orchestrating LifecycleHookServer, RunnerApiServer, state machine, workspace, pi process, lifecycle manager.
- Implement `scripts/dev/run-local.ts` local harness spinning up local bare git repo, fake secrets, local sink, and runner servers.
- Implement `scripts/dev/client.ts` driving SSE event stream, prompt submission, settlement waiting, and shutdown.
- Implement `scripts/e2e-local.ts` orchestrating the zero-cost e2e test with zero plaintext secrets assertion, manifest checks, git checkpoint checks, and metrics assertions.
- Add `tests/integration/e2e-local.test.ts` vitest suite and update `package.json` `e2e:local` script.
- Validate with `npm run e2e:local` and `npm run check`.

## Changes
- `runner/main.ts`: In-VM runner entrypoint and `RunnerServer` orchestrator class integrating hook server (9000), API server (8080), state machine, workspace preparation, pi process manager, and lifecycle manager.
- `runner/index.ts`: Exported `main.ts` module.
- `runner/lifecycle.ts`: Added `finalize()` and `createCheckpointCommit()` methods, serialized concurrent git commit operations via commit lock.
- `runner/hooks.ts`: Added `Logger` interface compatibility to `HookServerOptions`.
- `runner/api.ts`: Sent 200 shutting_down response before triggering deferred server teardown.
- `scripts/dev/run-local.ts`: Local development and test harness managing temporary run environment, bare git repo, fake secrets, and runner lifecycle.
- `scripts/dev/client.ts`: Dev client connecting to SSE stream, submitting prompt, streaming events, and invoking finalize/shutdown.
- `scripts/e2e-local.ts`: Zero-cost end-to-end local test runner verifying 5 primary criteria (manifest transitions, session mirroring, git checkpoint with hello.txt, tool metrics, zero secrets rule).
- `tests/integration/e2e-local.test.ts`: Vitest integration test suite for `e2e:local`.
- `package.json`: Configured `npm run e2e:local` script to run `tsx scripts/e2e-local.ts`.

## Validation

### npm run e2e:local
```
> pi-cloud-agents@0.1.0 e2e:local
> tsx scripts/e2e-local.ts

[E2E-LOCAL] Starting local harness (runId: run-e2e-1788734316434)...
[E2E-LOCAL] Harness ready on hook:9010, api:8090
[E2E-LOCAL] Submitting client prompt and listening to SSE stream...
[E2E-LOCAL] Client turn completed successfully with 25 events
[E2E-LOCAL] Finalizing run...

============================================================
Local End-to-End Test (T2.8): PASS
Run ID:   run-e2e-1788734316434
Duration: 1516ms
------------------------------------------------------------
Harness Startup:         PASS
Client Turn Stream:      PASS
Manifest Transitions:    PASS
Session Mirrored:        PASS
Git Checkpoint Created:  PASS
File Content (hello.txt):PASS
Metrics Captured:        PASS
Zero Secrets Plaintext:  PASS
============================================================
exit code: 0
```

### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts
Checked 77 files in 23ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit
 Test Files  25 passed (25)
      Tests  206 passed (206)
   Duration  6.87s
exit code: 0
```

### npm run test:integration
```
> pi-cloud-agents@0.1.0 test:integration
> vitest run tests/integration
 Test Files  2 passed (2)
      Tests  2 passed (2)
   Duration  1.94s
exit code: 0
```

## Notes / follow-ups
- All 5 assertions of T2.8 passed in 1.5 seconds locally with zero AWS costs.
- Ready to verify Gate G2 checklist.
