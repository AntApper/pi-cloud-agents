# T2.7 — Lifecycle policy (idle reporting, finalize, resume recovery, max duration)
- Agent/session: agent-t27   Started: 2026-09-06T22:29:00Z   Finished: 2026-09-06T22:30:00Z   Size est/actual: M / 20m

## Plan
- Implement `runner/lifecycle.ts`: `LifecyclePolicyManager` tracking attached clients, idle time, policy limits, and suggested actions (`none`, `suspend`, `terminate`).
- Implement automatic git WIP checkpoint commits (`pi-cloud: checkpoint <n>`) on `agent_settled` with time-boxed push if `autoPush` enabled.
- Implement pre-suspension and termination flushes within hook deadlines.
- Implement `/resume` recovery (state update, network refresh, and storage retry).
- Implement max duration warning event and checkpoint trigger at `maxDurationSec - 15 min`.
- Create unit tests in `tests/unit/runner-lifecycle.test.ts` and validate with `npm run check`.

## Changes
- files:
  - `runner/lifecycle.ts`
  - `runner/index.ts`
  - `tests/unit/runner-lifecycle.test.ts`
  - `docs/plan/STATUS.md`

## Validation
### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 71 files in 28ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  24 passed (24)
      Tests  200 passed (200)
```

## Notes / follow-ups
- `LifecyclePolicyManager` monitors idle state, attached connections, and advisory suggested action (`suspend` / `terminate`).
- Automated git WIP checkpoints (`pi-cloud: checkpoint <n>`) execute on `agent_settled` and update manifest git commit hash.
- Max lifetime duration warning emits at `maxDurationSec - 15 min` with automated session checkpointing.
