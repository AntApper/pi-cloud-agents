# T2.5b — Runner WebSocket RPC passthrough
- Agent/session: agent-t25b   Started: 2026-09-06T22:27:00Z   Finished: 2026-09-06T22:28:00Z   Size est/actual: M / 20m

## Plan
- Implement `runner/ws-rpc.ts` (and integrate with `RunnerApiServer`): WebSocket upgrade on `/v1/rpc` and `/ws/rpc`.
- Validate subprotocols (`lambda-microvms`, `lambda-microvms.authentication.<token>`, `lambda-microvms.port.8080`).
- Validate incoming client JSON frames (LF-delimited) and forward commands (`prompt`, `steer`, `abort`, etc.) to `PiProcessManager`.
- Implement event fan-out to all connected WebSocket clients with backpressure handling (drop delta `message_update` on overflowing send buffers, never drop terminal events).
- Track attached client (`attach` message) and route `extension_ui_request` with 120s auto-cancellation if unattached.
- Create unit/integration tests in `tests/unit/runner-ws-rpc.test.ts` and validate with `npm run check`.

## Changes
- files:
  - `runner/ws-rpc.ts`
  - `runner/api.ts`
  - `runner/index.ts`
  - `package.json`
  - `tests/unit/runner-ws-rpc.test.ts`
  - `docs/plan/STATUS.md`

## Validation
### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 69 files in 26ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  23 passed (23)
      Tests  194 passed (194)
```

## Notes / follow-ups
- Implemented `WebSocketRpcBridge` integrated with `RunnerApiServer`, supporting subprotocol negotiation, event fan-out, delta frame backpressure shedding, and attached UI client routing.
- Auto-cancellation responds to unattached or timed-out extension UI prompts cleanly.
