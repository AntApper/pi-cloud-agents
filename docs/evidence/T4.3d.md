# Evidence: T4.3d — Verification Engine and `/cloud verify`

## Plan
1. Implement `core/verify/engine.ts` with static checks (identity, region, stacks, S3, image, secrets, controller, permissions, config).
2. Support smoke run orchestration (boot MicroVM with mock-llm, prompt, tool call, auto-suspend, resume, cleanup) and optional model check.
3. Implement `extension/commands/verify.ts` and CLI integration with `stepList()` report and cost/timing summaries.
4. Implement unit tests in `tests/unit/verify-engine.test.ts` with mocked AWS SDK clients.
5. Validate `npm run check`.

## Output
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts
Checked 120 files in 36ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 ✓ tests/unit/verify-engine.test.ts (3 tests) 18ms
   ✓ Verification Engine (T4.3d) > runs all static checks, smoke run, and model check with green verdict
   ✓ Verification Engine (T4.3d) > identifies missing identity and resources with actionable remediations
   ✓ Verification Engine (T4.3d) > executes /cloud verify extension command handler

Test Files  42 passed (42)
     Tests  365 passed (365)
```
