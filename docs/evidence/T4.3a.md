# Evidence: T4.3a — Setup Wizard (UI + Config Step Machine)

## Plan
1. Implement `core/prompter.ts` abstract prompter interface (`select`, `multiselect`, `confirm`, `input`, `password`, `progress`).
2. Implement `extension/prompter-pi.ts` for pi UI/dialog interactions and `cli/prompter-terminal.ts` for standalone terminal CLI.
3. Build `core/setup/steps.ts` step machine with Quick Setup (default summary, $0/mo idle, detected profile/region, `Create` / `Customize` / `Cancel`) and Custom Wizard (preflight probe, AWS profile/region select, providers discovery & OAuth gating, GitHub PAT, sizing/budgets, confirmation).
4. Support `--dry-run` to output plan without disk write; generate validated `LocalConfig` and write to `~/.pi/agent/pi-cloud-agents.json`.
5. Implement unit tests in `tests/unit/setup-steps.test.ts` driving all branches with a scripted `Prompter`.

## Validation Output

```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

Checked 113 files in 32ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

 RUN  v3.2.7 /Users/ant/Documents/pi-cloud-agents

 ✓ tests/unit/setup-steps.test.ts (9 tests) 19ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > Discovery Helpers > discovers default or configured AWS profiles 2ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > Discovery Helpers > discovers local provider credentials categorizing api keys and oauth 1ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > Discovery Helpers > renders clean width-safe setup plan table without forbidden emojis 1ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > Quick Setup Flow (Default) > runs Quick Setup on 'create' action and saves default config 9ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > Quick Setup Flow (Default) > cancels cleanly when user selects 'cancel' 1ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > Quick Setup Flow (Default) > generates plan without saving when dryRun is enabled 1ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > Custom Wizard Flow > navigates all custom steps, gates OAuth with ToS confirmation, and records PAT 3ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > Custom Wizard Flow > strips OAuth provider from sync if user declines opt-in confirmation 2ms
   ✓ T4.3a Setup Wizard Step Machine & Prompter > handleCloudSetupCommand > executes setup via extension router and outputs plan result 1ms

 Test Files  39 passed (39)
      Tests  350 passed (350)
```

## Verdict
PASS. Setup wizard step machine and prompter abstractions implemented covering Quick Setup default flow, full Custom Wizard flow with OAuth ToS gating, dry-run support, and LocalConfig persistence.
