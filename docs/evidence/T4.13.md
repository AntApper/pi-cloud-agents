# Evidence: T4.13 — `/cloud sync`: Refresh the pi Config Bundle

## Plan
1. Implement `core/sync.ts` to discover local pi credentials, respect `providers.synced` and `providers.oauthOptIn`, assemble bundle TAR via `buildBundle()`, upload bundle to S3, and sync provider secrets to AWS Secrets Manager via `AwsSecretsStore`.
2. Implement `extension/commands/sync.ts` to handle `/cloud sync` and render a formatted summary table showing synced providers, OAuth status, and bundle size using UI kit.
3. Update `LocalConfig.providers.syncedAt` upon successful sync and save config.
4. Validate with unit tests in `tests/unit/sync.test.ts` with mocked AWS S3 & Secrets Manager.

## Validation Output

```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

Checked 107 files in 31ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

 RUN  v3.2.7 /Users/ant/Documents/pi-cloud-agents

 ✓ tests/unit/sync.test.ts (3 tests) 29ms
   ✓ T4.13 Cloud Sync (pi config bundle & credentials) > builds bundle, uploads to S3, syncs secrets to Secrets Manager, and updates syncedAt 24ms
   ✓ T4.13 Cloud Sync (pi config bundle & credentials) > gates OAuth sync based on oauthOptIn list 4ms
   ✓ T4.13 Cloud Sync (pi config bundle & credentials) > handles /cloud sync command via extension router and outputs summary table 10ms

 Test Files  38 passed (38)
      Tests  341 passed (341)
```

## Verdict
PASS. Pure core bundle sync engine and `/cloud sync` command implemented with S3 dual-key uploads (versioned + latest), Secrets Manager provider credential storage, OAuth opt-in gating, and LocalConfig syncedAt persistence.
