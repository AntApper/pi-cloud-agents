# T2.9 — Runner bundle, final Dockerfile, and deterministic image zip
- Agent/session: agent-t2.9   Started: 2026-09-06T22:40:00Z   Finished: 2026-09-06T22:42:00Z   Size est/actual: M / 30m

## Plan
- Update `image/Dockerfile` with AL2023 minimal base, pinned pi 0.85.1, runtime packages, askpass helper, runner entrypoint.
- Implement `scripts/build.ts` to bundle runner with esbuild (node22, esm, sourcemap) into `dist/runner/index.js` and generate `dist/controller.zip`.
- Implement `scripts/build-image-zip.ts` to create deterministic `dist/image/app.zip` (fixed mtimes, Dockerfile at root, runner, askpass, extensions) and `dist/image/manifest.json`.
- Add unit tests `tests/unit/build-artifacts.test.ts` verifying reproducible SHA256 hashes, zip contents, and size < 5 MB.
- Wire `npm run build` and validate with `npm run build`, `npm run check`, and `npm pack --dry-run`.

## Changes
- `image/Dockerfile`: Final version for Lambda MicroVM runtime based on AL2023 minimal ARM64 base, Node 22, Git, required packages, pinned pi 0.85.1, askpass helper, and runner entrypoint.
- `scripts/build.ts`: Build pipeline bundling runner to `dist/runner/index.js`, creating `dist/controller.zip`, and invoking image zip builder.
- `scripts/build-image-zip.ts`: Deterministic ZIP builder packing `Dockerfile` at root, `runner.js`, `askpass.sh`, and in-VM extensions with fixed epoch mtime `2026-01-01T00:00:00Z`, size validation (< 5 MB), and `dist/image/manifest.json` generation.
- `tests/unit/build-artifacts.test.ts`: Vitest unit tests asserting clean dist artifacts, reproducible byte-for-byte SHA256 hashes across consecutive builds, and zip file structure.
- `package.json`: Updated `build` script to execute `tsx scripts/build.ts`.

## Validation

### npm run build
```
> pi-cloud-agents@0.1.0 build
> tsx scripts/build.ts

Build completed successfully:
  Runner:     /Users/ant/Documents/pi-cloud-agents/dist/runner/index.js
  Controller: /Users/ant/Documents/pi-cloud-agents/dist/controller.zip
  Image ZIP:  /Users/ant/Documents/pi-cloud-agents/dist/image/app.zip (355292 bytes, SHA256: fb05ac02e038c866c90e4a5baf75ccaa34f1796a5711c5bde179c66f60d59490)
exit code: 0
```

### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts
Checked 79 files in 27ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit
 Test Files  26 passed (26)
      Tests  209 passed (209)
   Duration  7.29s
exit code: 0
```

### npm pack --dry-run
```
npm notice 📦  pi-cloud-agents@0.1.0
npm notice Tarball Contents
npm notice 258B dist/controller.zip
npm notice 355.3kB dist/image/app.zip
npm notice 177B dist/image/manifest.json
npm notice 1.9MB dist/runner/index.js
npm notice 3.2MB dist/runner/index.js.map
npm notice 1.2kB image/Dockerfile
...
exit code: 0
```

## Notes / follow-ups
- Image artifact `dist/image/app.zip` is 355 KB (limit < 5 MB).
- Unblocks Task T3.3 (Image Manager) in Phase 3.
