# T5.10 — Post-release review fixes: packaging, IAM hygiene, bounded pagination
- Agent/session: review-fixes-agent   Started: 2026-09-07T01:30:00Z   Finished: (in progress)   Size est/actual: M / —

## Plan
- Add `core/aws/paginate.ts` (page cap, stuck-token guard, `AbortSignal`) and route every AWS list
  call in cleanup, controller, status, launcher, list and image pruning through it.
- Fix CLI packaging: external runtime deps, no inlined `esbuild`/`scripts/build.ts`, no shipped
  sourcemap, `prepublishOnly`, `BuildSummary.cliBundlePath`, pack test.
- IAM: wire `KmsKeyArn` into the image stack from setup, add `kms:GenerateDataKey*`, document the
  `ListSecrets` wildcard, extend the wildcard guard test to the controller role.
- One default stack name; runner mode echo; steer next-step hint; tar hardening; stronger tests.

## Review findings addressed (source: multi-model review of `d351dab`, `20a6001`)
1. `bin` pointed at gitignored `dist/cli/main.js` with no publish guard.
2. CLI bundle inlined `esbuild`, `scripts/build.ts` and all `@aws-sdk/*` (3.8 MB + 6.0 MB map).
3. `secretsmanager:ListSecrets` on `Resource: "*"` added without documentation or guard test.
4. Controller KMS statement unreachable (parameter never passed) and missing `GenerateDataKey*`.
5. Single-page list calls left in `core/launcher.ts`, `core/list.ts`, `core/aws/image.ts`.
6. New `do...while` pagination loops unbounded; `resolveRunId` walked every object under `runs/`.
7. `emptyAndDeleteS3Bucket` swallowed every error into the unversioned fallback.
8. Weak tests (single-page `ListSecrets` mock, injected `gitRunner` only, no `HasKmsKey` assert).
9. `follow_up` alias echoed raw and undocumented.
10. Steer failure text lacked "what to do next".
11. Two different default stack names (`core/sync.ts` vs `shared/config.ts`).
12. `extractTar` lexical-only guard; ustar `prefix`, GNU `L` and PAX `x`/`g` entries mishandled.

## Changes
- (filled in at the end)

## Validation
- (filled in at the end)
