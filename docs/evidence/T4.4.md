# Evidence: T4.4 — `/cloud new` Launch Flow

## Plan
1. Implement `core/launcher.ts` with git inspection, model validation, LaunchPayload formation, concurrency checking, S3 persistence, MicroVM launch, and prompt dispatch.
2. Implement `extension/commands/new.ts` for `/cloud new [prompt]` command.
3. Wire route in `extension/router.ts`.
4. Implement unit tests in `tests/unit/launcher.test.ts` covering payload formation, dry-run, concurrency cap, dirty tree warning, and failure handling.
5. Validate `npm run check`.

## Output
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts
Checked 123 files in 41ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 ✓ tests/unit/launcher.test.ts (7 tests) 743ms
   ✓ Cloud Agent Launcher (T4.4) > generateRunId > generates valid runId matching RUN_ID_REGEX
   ✓ Cloud Agent Launcher (T4.4) > inspectGitRepo > inspects current repository without throwing
   ✓ Cloud Agent Launcher (T4.4) > launchCloudRun > launches a cloud run end-to-end with dry-run mode
   ✓ Cloud Agent Launcher (T4.4) > launchCloudRun > enforces maxConcurrent runs limit and throws ConcurrencyLimitExceeded
   ✓ Cloud Agent Launcher (T4.4) > launchCloudRun > rejects launch when model provider is not synced and Bedrock role disabled
   ✓ Cloud Agent Launcher (T4.4) > launchCloudRun > updates manifest to failed when RunMicrovmCommand fails
   ✓ Cloud Agent Launcher (T4.4) > Extension /cloud new command > displays usage guidance when prompt argument is empty

Test Files  43 passed (43)
     Tests  372 passed (372)
```
