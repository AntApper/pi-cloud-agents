# T3.3 Evidence: Lambda MicroVM Image Manager, Polling, Diagnostics, and Pruning

## Plan
1. Implement `core/aws/image.ts` with `uploadArtifactIfMissing` (content-addressed S3 skip), `resolveLatestBaseImage`, and `MicrovmImageManager`.
2. Implement `deployImageArtifacts`, `waitForImageReady` (polling status with CloudWatch build log extraction on failure), `describeImage`, `checkImageDrift`, and `pruneVersions` (retaining top active versions).
3. Author comprehensive unit tests in `tests/unit/aws-image.test.ts` using `aws-sdk-client-mock`.
4. Validate full suite via `npm run check`.
5. Record validation output, update `STATUS.md` to `done`, and commit.
