# T2.4 — pi process manager (RPC bridge)
- Agent/session: agent-t24   Started: 2026-09-06T22:20:00Z   Finished: 2026-09-06T22:20:30Z   Size est/actual: M / 30m

## Plan
- Implement `tests/fakes/fake-pi.ts` handling RPC commands (`prompt`, `get_state`, `abort`, `__CRASH__`) and emitting LF JSONL events.
- Implement `runner/pi-process.ts` with LF-only JSONL stream parser, request/response correlation, event fanout, crash recovery (one restart with `--session <file>`, second fails with `PI_PROCESS_CRASHED`), and graceful stop (`abort` -> SIGTERM -> SIGKILL).
- Write `tests/unit/runner-pi-process.test.ts` testing RPC flows, U+2028/U+2029 newline safety, crash recovery, and graceful shutdown.
- Validate with `npm run check`.

## Changes
- files:
  - `tests/fakes/fake-pi.ts`
  - `runner/pi-process.ts`
  - `runner/index.ts`
  - `tests/unit/runner-pi-process.test.ts`
  - `docs/plan/STATUS.md`

## Validation
### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 63 files in 22ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  20 passed (20)
      Tests  169 passed (169)
```

## Notes / follow-ups
- LF-only stream parser strictly splits on byte `0x0A`, preventing Unicode line/paragraph separators (U+2028/U+2029) inside JSON payloads from corrupting records.
- Crash recovery preserves conversation continuity by restarting with `--session <file>` on first crash.
- Unblocks T2.5a (Runner HTTP API).
