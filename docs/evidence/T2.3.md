# T2.3 — Workspace preparation (git + install)
- Agent/session: agent-t23   Started: 2026-09-06T22:19:00Z   Finished: 2026-09-06T22:19:30Z   Size est/actual: M / 30m

## Plan
- Implement `runner/workspace.ts` with git clone, branch creation/checkout, user configuration, and GIT_ASKPASS credentialing.
- Implement install command execution via `bash -lc` with timeout, partial output capture, and `INSTALL_TIMEOUT` error.
- Implement detached start process spawner and archive restore interface for continuation.
- Create `tests/unit/runner-workspace.test.ts` covering local bare clone, security assertion (.git/config no token), install success/timeout, and archive restore.
- Run `npm run check` to verify types, lint, and tests.

## Changes
- files:
  - `runner/workspace.ts`
  - `runner/index.ts`
  - `tests/unit/runner-workspace.test.ts`
  - `docs/plan/STATUS.md`

## Validation
### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 60 files in 22ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  19 passed (19)
      Tests  163 passed (163)
```

## Notes / follow-ups
- Verified that GIT_ASKPASS helper prevents token leakage into `.git/config` and remote origin URL.
- Structured timeout `InstallTimeoutError` captures partial build log before SIGKILL.
- Continuation interface `restoreWorkspaceFromArchive` extracts tarballs into working directories.
