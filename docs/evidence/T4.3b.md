# Evidence: T4.3b — Setup Execution Engine

## Plan
1. Implement `core/setup/run.ts` with resumable step ledger (`pi-cloud-setup-ledger.json`).
2. Orchestrate 8-step flow: core stack, artifacts upload, image stack, image ready wait, bundle/secrets sync, GitHub token, config write, verify.
3. Handle failure resiliency with `MappedAwsError` and cancel safety.
4. Wire setup execution into `/cloud setup` command and standalone CLI.
5. Add comprehensive unit tests in `tests/unit/setup-run.test.ts` with `aws-sdk-client-mock`.
6. Validate with `npm run check`.

## Output
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts
Checked 115 files in 34ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 ✓ tests/unit/setup-run.test.ts (4 tests) 47ms
   ✓ Setup Execution Engine (T4.3b) > executes full setup workflow from scratch (all 8 steps) successfully
   ✓ Setup Execution Engine (T4.3b) > resumes from existing ledger skipping already completed steps
   ✓ Setup Execution Engine (T4.3b) > handles dry-run mode without executing AWS changes
   ✓ Setup Execution Engine (T4.3b) > captures deployment failures, records partial ledger, and returns actionable MappedAwsError

Test Files  40 passed (40)
     Tests  354 passed (354)
```
