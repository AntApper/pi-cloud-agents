# T5.11 — `/cloud update` passes the image stack its declared parameters
- Agent/session: review-fixes-agent   Started: 2026-09-07T01:40:00Z   Finished: 2026-09-07T01:55:00Z   Size est/actual: S / S

## Plan
- Reproduce the mismatch: `core/lifecycle-ops.ts` sent `CoreStackName`, `RunnerZipKey`,
  `ControllerZipKey`; `infra/image.yaml` declares `ArtifactBucket`, `RunnerArtifactKey`,
  `ControllerArtifactKey`, `ImageName`, `MemoryMiB`, `BuildRoleArn`, `ExecutionRoleArn`,
  `BaseImageArn`, `BaseImageVersion`, `ImageLogGroup`, `KmsKeyArn`.
- Derive the map from the core stack outputs the same way `core/setup/run.ts` does.
- Fail before any upload when `BuildRoleArn` / `ExecutionRoleArn` outputs are missing.
- Pin the contract with a test that compares the change-set parameter keys to the template.

## Changes
- `core/lifecycle-ops.ts`: `executeCloudUpdate` reads `BuildRoleArn`, `ExecutionRoleArn`,
  `ImageLogGroup` from the core stack outputs, throws `CORE_OUTPUTS_MISSING` with the repair step
  (`/cloud setup`) when the role ARNs are absent, and deploys `infra/image.yaml` with exactly its
  declared parameters, including `KmsKeyArn` from the local config (empty string when unset).
- `tests/fakes/cfn-template.ts`: `declaredTemplateParameters()` reads the `Parameters` block of a
  template so tests assert against the template instead of a hand-copied list.
- `tests/unit/cloud-update-destroy.test.ts`: asserts the `CreateChangeSet` parameter keys equal the
  template's declared parameters and carry the derived values; asserts the early failure happens
  before `PutObject`.

## Validation
- `npx vitest run tests/unit/cloud-update-destroy.test.ts`: green (parameter-key equality and
  early-failure cases included).
- `npm run check`: green — 63 files, 504 tests (shared run with T5.10).
- No AWS resources were touched; the fix is exercised against `aws-sdk-client-mock` only. First live
  `/cloud update` after this change should be watched once (G4-style smoke) — recorded in JOURNAL.
