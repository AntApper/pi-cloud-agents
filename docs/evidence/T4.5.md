# Evidence: T4.5 — Run Client (Tokens, HTTP, SSE, WS)

## Plan
1. Implement `core/client/run-client.ts` with proxy token minting (`CreateMicrovmAuthToken`), auto-refresh at T-5m, and 403 single retry.
2. Build HTTP client handling `x-aws-proxy-auth`, `x-aws-proxy-port: 8080`, and 502 auto-resume backoff with jitter up to 60s.
3. Build typed SSE stream consumer with `Last-Event-ID` cursor management and auto-reconnect.
4. Implement WebSocket RPC client with subprotocol negotiation (`lambda-microvms`, `lambda-microvms.authentication.<token>`, `lambda-microvms.port.8080`) and command/event routing.
5. Add periodic 60s keepalive/RTT polling against `GET /v1/status` per ADR-4.
6. Validate with unit and integration test suite in `tests/unit/run-client.test.ts` across all protocols.

## Validation Output

```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

Checked 101 files in 31ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

 RUN  v3.2.7 /Users/ant/Documents/pi-cloud-agents

 ✓ tests/unit/run-client.test.ts (11 tests) 1743ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > Token Management & Auto-Refresh > mints proxy auth token via CreateMicrovmAuthToken and caches it 10ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > Token Management & Auto-Refresh > auto-refreshes token at T-5 minutes before expiration 2ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > Token Management & Auto-Refresh > handles forceRefresh flag to immediately mint a new token 1ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > HTTP Fetch Wrapper & Error Handling > attaches X-aws-proxy-auth and X-aws-proxy-port headers to requests 2ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > HTTP Fetch Wrapper & Error Handling > re-mints token and retries once on HTTP 403 Forbidden 1ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > HTTP Fetch Wrapper & Error Handling > retries with exponential backoff on HTTP 502 (MicroVM auto-resuming) 1524ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > HTTP Fetch Wrapper & Error Handling > throws RunClientHttpError on unrecoverable HTTP failure 1ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > Typed REST Endpoints > correctly executes all REST methods against server 34ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > SSE Streaming & Reconnect Handling > subscribes to SSE stream, parses typed envelopes, tracks cursor, and triggers onSettled 9ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > WebSocket RPC & Subprotocol Negotiation > connects with lambda-microvms subprotocols and sends RPC commands 35ms
   ✓ T4.5 RunClient (Tokens, HTTP, SSE, WS) > Attached Keepalive & RTT Polling > periodically polls GET /v1/status and computes RTT 107ms

 Test Files  36 passed (36)
      Tests  327 passed (327)
```

## Verdict
PASS. Full client abstraction implemented covering token lifecycle, resilient HTTP request/retry mechanics, SSE streams, and WebSocket RPC passthrough.
