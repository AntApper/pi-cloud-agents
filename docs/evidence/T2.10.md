# T2.10 — Runner metrics and lifecycle timeline
- Agent/session: agent-t210   Started: 2026-09-06T22:31:00Z   Finished: 2026-09-06T22:32:00Z   Size est/actual: M / 25m

## Plan
- Implement `runner/metrics.ts`: `MetricsCollector` tracking lifecycle timeline, agent counters (turns, tool calls by tool), model TTFT and turn timings, workspace git stats, and VM resource ring buffer (5s sample rate, 6h window).
- Implement events/min series for dashboard sparklines (30m window).
- Enforce strict real-data rule: omit fields that cannot be measured on the host OS rather than fabricating fake placeholder numbers.
- Expose summary in `/v1/status` and full payload in `GET /v1/metrics`, plus periodic `metrics` SSE frame.
- Create unit tests in `tests/unit/runner-metrics.test.ts` and validate with `npm run check`.

## Changes
- files:
  - `runner/metrics.ts`
  - `runner/api.ts`
  - `runner/index.ts`
  - `tests/unit/runner-metrics.test.ts`
  - `docs/plan/STATUS.md`

## Validation
### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 73 files in 22ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  25 passed (25)
      Tests  206 passed (206)
```

## Notes / follow-ups
- Implemented `MetricsCollector` adhering to `07-ux-and-observability.md §3` catalogue and the real-data omission rule.
- Collected turn timings, TTFT, tool call counters, git diff metrics, and 5s VM resource sampling across a 6h ring buffer.
- Integrated with `RunnerApiServer` serving `GET /v1/metrics`.
