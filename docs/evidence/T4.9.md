# Evidence: T4.9 — `cloud_agent` tool for the local LLM

## Plan
1. Implement `extension/tools/cloud-agent.ts` with `pi.registerTool` for `cloud_agent`.
2. Support actions `launch`, `status`, `result`, `steer`, `stop` with parameter validation.
3. Enforce output truncation (50 KB / 2,000 lines) and renderCall/renderResult UI formatters.
4. Hook tool registration into `extension/index.ts`.
5. Implement unit test suite `tests/unit/tool-cloud-agent.test.ts` covering all actions and edge cases.
6. Validate with `npm run check` and record outputs.

## Validation Output
*Validation in progress...*
