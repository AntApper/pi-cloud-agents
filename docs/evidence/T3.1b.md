# T3.1b Evidence: CloudFormation Image Stack & Controller Schedule

## Plan
1. Author `infra/image.yaml` configuring `AWS::Lambda::MicrovmImage` with all required properties and explicit hook timeouts.
2. Define Node 22 ARM64 Controller Lambda with reserved concurrency 1 and EventBridge 1-minute schedule.
3. Configure Controller IAM execution role scoped to image operations, S3 manifests, and run secret janitoring.
4. Update `tests/unit/infra-policies.test.ts` to validate `infra/image.yaml` resource properties, hook timeouts, and IAM policies.
5. Validate via `npm run check` and document results.

## Validation Output

### 1. `tests/unit/infra-policies.test.ts`
```
 RUN  v3.2.7 /Users/ant/Documents/pi-cloud-agents

 ✓ tests/unit/infra-policies.test.ts (15 tests) 5ms

 Test Files  1 passed (1)
      Tests  15 passed (15)
   Duration  280ms
exit code: 0
```

### 2. `npm run check`
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 80 files in 22ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  27 passed (27)
      Tests  224 passed (224)
   Duration  6.93s
exit code: 0
```

## Security & Architecture Verification Checklist
- `infra/image.yaml` declares `AWS::Lambda::MicrovmImage` with all required properties (`Name`, `Description`, `BaseImageArn`, `BaseImageVersion`, `BuildRoleArn`, `CodeArtifact`, `CpuConfigurations` [ARM_64], `AdditionalOsCapabilities`, `EgressNetworkConnectors`, `EnvironmentVariables`, `Hooks`, `Logging`, `Resources`, `Tags`).
- Explicit hook timeouts configured: Ready (120s), Validate (120s), Run (30s), Resume (15s), Suspend (45s), Terminate (45s).
- Node 22 ARM64 Controller Lambda function defined with reserved concurrency 1, timeout 50s, EventBridge 1-minute schedule (`rate(1 minute)`).
- Controller IAM execution role grants scoped `lambda:{ListMicrovms,GetMicrovm,SuspendMicrovm,TerminateMicrovm,CreateMicrovmAuthToken}`, S3 manifest access, and `secretsmanager:DeleteSecret` on `runs/*`.
