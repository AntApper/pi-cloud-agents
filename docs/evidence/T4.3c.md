# Evidence: T4.3c — Standalone CLI (`npx pi-cloud-agents ...`)

## Plan
1. Implement `cli/main.ts` entrypoint supporting setup, verify, doctor, config, sync, update, destroy.
2. Support non-interactive CLI flags: `--dry-run`, `--non-interactive`, `--config`, `--verify`, `--with-model`, `--json`.
3. Support non-pi credential discovery via direct `~/.pi/agent/auth.json` reading and environment variables.
4. Create fixture `tests/fixtures/setup.json` and validate `npx . setup --dry-run --non-interactive --config tests/fixtures/setup.json`.
5. Implement unit tests in `tests/unit/cli.test.ts` for arg parsing, dry-run, and exit codes.
6. Validate `npm run check`.

## Output
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts
Checked 117 files in 35ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 ✓ tests/unit/cli.test.ts (8 tests) 1028ms
   ✓ Standalone CLI Tool (T4.3c) > parseCliArgs > parses commands, sub-arguments, and long/short flags accurately
   ✓ Standalone CLI Tool (T4.3c) > parseCliArgs > handles help and version flags
   ✓ Standalone CLI Tool (T4.3c) > discoverCredentialsWithoutPi > reads credentials from auth.json and supplements from environment variables
   ✓ Standalone CLI Tool (T4.3c) > runCli commands > executes 'version' command with text and JSON output
   ✓ Standalone CLI Tool (T4.3c) > runCli commands > executes 'help' command
   ✓ Standalone CLI Tool (T4.3c) > runCli commands > executes 'setup --dry-run --non-interactive --config tests/fixtures/setup.json'
   ✓ Standalone CLI Tool (T4.3c) > runCli commands > returns exit code 1 for missing configuration file
   ✓ Standalone CLI Tool (T4.3c) > runCli commands > returns exit code 1 for unknown command

Test Files  41 passed (41)
     Tests  362 passed (362)
```

CLI Dry-Run Execution with Fixture:
```
$ npx tsx cli/main.ts setup --dry-run --non-interactive --config tests/fixtures/setup.json
┌ pi cloud agents · Setup Plan ────────────────────────────────────────────┐
│ AWS Profile:        (default) ((active profile))                         │
│ AWS Region:         us-east-1                                            │
│ Runner Image:       pi-cloud-agents-runner (4096 MiB RAM)                │
│ Max Duration:       4 hours (idle suspend: 15m)                          │
│ Concurrency Limit:  3 active runs                                        │
├──────────────────────────────────────────────────────────────────────────┤
│ Synced Providers:   anthropic, openai                                    │
│ OAuth Opt-Ins:      (none)                                               │
│ Amazon Bedrock:     Enabled                                              │
│ GitHub PAT:         None (public repos only)                             │
├──────────────────────────────────────────────────────────────────────────┤
│ Estimated Idle Cost: $0.00 / month (zero cost when no runs active)       │
└──────────────────────────────────────────────────────────────────────────┘

Plan preview generated (dry-run). No changes were made.
```
