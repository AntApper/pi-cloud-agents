# T2.6 — Run state machine and persistence
- Agent/session: agent-t26   Started: 2026-09-06T22:21:00Z   Finished: 2026-09-06T22:21:30Z   Size est/actual: M / 30m

## Plan
- Implement `RunStateMachine` in `runner/state.ts` enforcing valid lifecycle transitions, timeline tracking, usage/git updates, and manifest schema validation.
- Implement storage persistence with automatic manifest writing and debounced session mirroring (5s) with immediate flush on `agent_settled`, `suspend`, and `terminate`.
- Ensure strict termination flush ordering: `session.jsonl` flushed before final `terminated` / `failed` manifest.
- Create `tests/unit/runner-state.test.ts` testing state transitions, illegal transition rejection, timeline accumulation, session mirroring, and flush ordering.
- Run `npm run check` to verify types, lint, and tests.

## Changes
- files:
  - `runner/state.ts`
  - `runner/index.ts`
  - `tests/unit/runner-state.test.ts`
  - `docs/plan/STATUS.md`

## Validation
### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 65 files in 23ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  21 passed (21)
      Tests  176 passed (176)
```

## Notes / follow-ups
- Implemented state transition table and validation enforcing that only legal transitions can execute.
- Session file mirroring debounces stream chunks and guarantees full session flush before terminal manifest write.
- Retry logic with exponential backoff handles transient storage write failures.
