# T4.1a Evidence: Extension Skeleton, /cloud Router, and /cloud Doctor

## Plan
1. Implement `extension/router.ts` registering `/cloud <sub> [args]` with completions for 20 subcommands and run IDs.
2. Implement `/cloud help` catalog and stub unimplemented subcommands with clean notices.
3. Implement `/cloud doctor` probing config, AWS identity (masked), region, stacks, image drift, pi version, and concurrency.
4. Implement footer status badge (`cloud n running · m idle`) refreshed every 30s while idle, guarded with `ctx.hasUI`.
5. Author unit tests in `tests/unit/extension-router.test.ts` and `tests/unit/extension-doctor.test.ts`.
6. Validate via `npm run check`.

## Validation Output

### 1. `tests/unit/extension-router.test.ts` & `tests/unit/extension-doctor.test.ts`
```
 RUN  v3.2.7 /Users/ant/Documents/pi-cloud-agents

 ✓ tests/unit/extension-router.test.ts (9 tests) 30ms
 ✓ tests/unit/extension-doctor.test.ts (3 tests) 18ms
 ✓ tests/unit/extension.test.ts (2 tests) 3ms

 Test Files  3 passed (3)
      Tests  14 passed (14)
   Duration  240ms
exit code: 0
```

### 2. Headless RPC Execution (`get_commands` and `/cloud doctor`)
```
Captured: {"type":"extension_ui_request","method":"setStatus","statusKey":"cloud","statusText":"cloud 0 running · 0 idle"}
{"type":"response","command":"get_commands","success":true,"data":{"commands":[{"name":"cloud","description":"Manage AWS Lambda MicroVM cloud agents","source":"extension"}]}}

Captured doctor output:
┌ pi cloud agents · Doctor Diagnostics ────────────────────────────────────┐
│ Configuration File   Not configured (run /cloud setup to initi…   ▲ WARN │
│ AWS Identity         Could not load credentials from any provi…   ✗ FAIL │
│ Region Support       us-east-1 (MicroVMs available)               ✓ PASS │
│ Core Stack           Could not load credentials from any provi…   ✗ FAIL │
│ Runner Image         Could not load credentials from any provi…   ✗ FAIL │
│ pi Target Version    Pinned 0.85.1                                ✓ PASS │
│ Synced Providers     None configured                              ▲ WARN │
│ Max Concurrency      3 concurrent run(s)                          ✓ PASS │
├──────────────────────────────────────────────────────────────────────────┤
│ Verdict: BROKEN                                                 ✗ BROKEN │
├ Remediations ──────────────────────────────────────────────────────────┤
│ → Run '/cloud setup' or 'npx pi-cloud-agents setup' to create initial    │
│   configuration.                                                         │
│ → Ensure AWS credentials are configured via AWS_PROFILE or               │
│   ~/.aws/credentials.                                                    │
│ → Run '/cloud setup' to create the CloudFormation infrastructure.        │
│ → Build image with '/cloud setup'.                                       │
│ → Run '/cloud sync' to sync local provider API keys.                     │
└──────────────────────────────────────────────────────────────────────────┘
```

### 3. `npm run check`
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 94 files in 52ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  33 passed (33)
      Tests  286 passed (286)
   Duration  7.66s
exit code: 0
```

## Security & Architecture Verification Checklist
- All 20 subcommands cataloged and argument completions provided for subcommands and run IDs.
- `/cloud doctor` probes configuration, AWS identity (account and ARN masked), regional availability, CloudFormation stacks, image drift, pinned pi version, synced provider credentials, and concurrency limits.
- Footer status badge updated to `cloud n running · m idle` (theme muted, non-blocking background refresh).
- Zero emoji used across all routers, help catalogs, tables, and notices.
- All UI interactions guarded with `ctx.hasUI`.
- Task T4.1a complete; unblocks T4.1b (UI kit) and T4.3a (Setup wizard).
