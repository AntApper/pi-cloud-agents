# T4.2 Evidence: AWS Client Factory and Error Mapping

## Plan
1. Implement `core/aws/clients.ts` with `AwsClientFactory` memoizing AWS SDK clients, supporting credential profile resolution, and exponential backoff retry.
2. Implement `mapAwsError` translating AWS SDK errors (IAM access denied, service quotas, expired tokens, unsupported regions, not found, network timeouts) into actionable user-facing messages.
3. Export extension wrapper in `extension/aws/clients.ts`.
4. Author comprehensive unit tests in `tests/unit/aws-clients.test.ts`.
5. Validate via `npm run check`.

## Validation Output

### 1. `tests/unit/aws-clients.test.ts`
```
 RUN  v3.2.7 /Users/ant/Documents/pi-cloud-agents

 ✓ tests/unit/aws-clients.test.ts (11 tests) 14ms
   ✓ AWS Client Factory & Error Mapping (T4.2) > AwsClientFactory Memoization & Lifecycle > creates and memoizes AWS SDK client instances
   ✓ AWS Client Factory & Error Mapping (T4.2) > AwsClientFactory Memoization & Lifecycle > partitions cache keys by region and profile
   ✓ AWS Client Factory & Error Mapping (T4.2) > AwsClientFactory Memoization & Lifecycle > clears cached client instances
   ✓ AWS Client Factory & Error Mapping (T4.2) > AwsClientFactory Memoization & Lifecycle > creates client factory pre-configured from LocalConfig
   ✓ AWS Client Factory & Error Mapping (T4.2) > mapAwsError Actionable Error Mapping > maps IAM AccessDeniedException to ACCESS_DENIED with OperatorPolicy guidance
   ✓ AWS Client Factory & Error Mapping (T4.2) > mapAwsError Actionable Error Mapping > maps ServiceQuotaExceededException to QUOTA_EXCEEDED with console tips
   ✓ AWS Client Factory & Error Mapping (T4.2) > mapAwsError Actionable Error Mapping > maps ExpiredToken to EXPIRED_CREDENTIALS with re-authentication tips
   ✓ AWS Client Factory & Error Mapping (T4.2) > mapAwsError Actionable Error Mapping > maps unsupported regions to UNSUPPORTED_REGION with supported region list
   ✓ AWS Client Factory & Error Mapping (T4.2) > mapAwsError Actionable Error Mapping > maps ResourceNotFoundException to RESOURCE_NOT_FOUND
   ✓ AWS Client Factory & Error Mapping (T4.2) > mapAwsError Actionable Error Mapping > maps network connectivity failures to NETWORK_ERROR
   ✓ AWS Client Factory & Error Mapping (T4.2) > mapAwsError Actionable Error Mapping > preserves already mapped MappedAwsError instances idempotently

 Test Files  1 passed (1)
      Tests  11 passed (11)
   Duration  140ms
exit code: 0
```

### 2. `npm run check`
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 99 files in 29ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  35 passed (35)
      Tests  316 passed (316)
   Duration  7.43s
exit code: 0
```

## Security & Architecture Verification Checklist
- `AwsClientFactory` memoizes AWS SDK client instances by client type, region, and profile.
- Credentials resolved from `LocalConfig` or `process.env.AWS_PROFILE` with adaptive exponential backoff retries.
- `mapAwsError` maps raw errors into actionable messages following *what failed -> why -> what to do next* (§1.0 principle 6).
- Zero secrets logged in error mappings or messages; account IDs and tokens masked with `maskAccountId`/`maskArn`.
- Core isolation rule verified: `core/aws/clients.ts` does not import `@earendil-works/pi-coding-agent`.
- Task T4.2 complete; unblocks T4.3a (Setup wizard) and T4.5 (Run client).
