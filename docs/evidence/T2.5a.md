# T2.5a — Runner HTTP API (REST + SSE)
- Agent/session: agent-t25a   Started: 2026-09-06T22:25:00Z   Finished: 2026-09-06T22:26:00Z   Size est/actual: M / 25m

## Plan
- Implement `runner/api.ts` HTTP server on port 8080 (REST routes + SSE stream).
- Support `GET /healthz`, `GET /v1/status`, `GET /v1/manifest`, `GET /v1/entries?since=`, `GET /v1/events` (SSE with replay and 15s heartbeats), `POST /v1/prompt` (with 409 when streaming), `POST /v1/interrupt`/`POST /v1/abort`, `POST /v1/checkpoint`, `POST /v1/finalize`/`POST /v1/shutdown`.
- Enforce 1 MB request body limit, JSON error responses, and proxy header validation.
- Export route registry and sync `shared/protocol.ts` and `docs/protocol.md` for complete contract parity.
- Create comprehensive unit and integration tests in `tests/unit/runner-api.test.ts`.
- Validate with `npm run check`.

## Changes
- files:
  - `runner/api.ts`
  - `runner/index.ts`
  - `runner/state.ts`
  - `shared/protocol.ts`
  - `docs/protocol.md`
  - `tests/unit/runner-api.test.ts`
  - `docs/plan/STATUS.md`

## Validation
### npm run check
```
> pi-cloud-agents@0.1.0 check
> npm run typecheck && npm run lint && npm run test:unit

> pi-cloud-agents@0.1.0 typecheck
> tsc --noEmit

> pi-cloud-agents@0.1.0 lint
> biome check . && node --experimental-strip-types scripts/doc-glyph-scan.ts

Checked 67 files in 22ms. No fixes applied.
Glyph scan passed: no forbidden emojis or symbols found.

> pi-cloud-agents@0.1.0 test:unit
> vitest run tests/unit

 Test Files  22 passed (22)
      Tests  188 passed (188)
```

## Notes / follow-ups
- Exported `RunnerApiServer` supporting complete REST API, SSE streaming with historical replay and heartbeats, 409 Conflict protection on streaming prompts, and 1 MB payload limits.
- Protocol routes synchronized across `shared/protocol.ts`, `docs/protocol.md`, and `tests/unit/protocol-doc.test.ts`.
